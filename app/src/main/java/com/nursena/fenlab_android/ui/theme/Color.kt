package com.nursena.fenlab_android.ui.theme

import androidx.compose.ui.graphics.Color

// ── Arka Plan Katmanları ──────────────────────────────────────────────────────
val DarkBg        = Color(0xFF0A0E1A)   // en derin arka plan
val DarkSurface   = Color(0xFF131929)   // kart yüzeyi
val DarkSurface2  = Color(0xFF1C2333)   // ikincil yüzey / chip bg
val DarkSurface3  = Color(0xFF242B3D)   // üçüncül (input, border)

// ── Teal Aksanlar ─────────────────────────────────────────────────────────────
val Teal400       = Color(0xFF26C6B0)   // parlak teal (highlight)
val Teal500       = Color(0xFF00897B)   // ana teal
val Teal700       = Color(0xFF00695C)
val Teal100       = Color(0xFF1A3D38)   // koyu modda teal container

// ── Turuncu ───────────────────────────────────────────────────────────────────
val Orange400     = Color(0xFFFFAB40)
val Orange500     = Color(0xFFFF8F00)
val Orange100     = Color(0xFF3D2800)   // koyu modda orange container

// ── Metin ─────────────────────────────────────────────────────────────────────
val TextPrimary   = Color(0xFFF0F4FF)   // neredeyse beyaz
val TextSecondary = Color(0xFF8892A4)   // soluk mavi-gri
val TextTertiary  = Color(0xFF4A5568)   // çok soluk

// ── Chip Renkleri (ders) ──────────────────────────────────────────────────────
val ChipScience   = Color(0xFF0D4A3A)   // Fen Bilimleri — koyu yeşil
val ChipPhysics   = Color(0xFF1A2B5C)   // Fizik — koyu mavi
val ChipChemistry = Color(0xFF3D1A00)   // Kimya — koyu turuncu
val ChipBiology   = Color(0xFF1A3D1A)   // Biyoloji — koyu yeşil
val ChipOther     = Color(0xFF2D2D2D)

val ChipScienceText   = Color(0xFF4ECCA3)
val ChipPhysicsText   = Color(0xFF7EB8F7)
val ChipChemistryText = Color(0xFFFFAB40)
val ChipBiologyText   = Color(0xFF81C784)
val ChipOtherText     = Color(0xFF9E9E9E)

// ── Status ────────────────────────────────────────────────────────────────────
val Red400        = Color(0xFFEF5350)
val Green400      = Color(0xFF66BB6A)
val Yellow400     = Color(0xFFFFCA28)

// ── Eski isimler (geriye dönük uyumluluk) ─────────────────────────────────────
val Teal50        = Color(0xFF0D1F1D)
val Gray900       = TextPrimary
val Gray700       = TextSecondary
val Gray400       = TextTertiary
val Gray100       = DarkSurface2
val Gray50        = DarkSurface
val Red500        = Red400
val Green500      = Green400
val Orange100_old = Orange100